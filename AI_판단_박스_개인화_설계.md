# AI 판단 박스 개인화 설계

> 뉴스 상세 화면 "AI는 이렇게 판단했어요" 박스 관련 설계 문서.
> 변경사항이 생길 때마다 이 문서를 계속 업데이트한다. (하단 변경 이력 참고)

## 배경 / 컨셉

이 박스는 "안심시켜주는 도구"가 아니라 **"놓치지 않게 챙겨주는 도구"**로 포지셔닝한다.
사용자가 아무 행동을 하지 않아도 화면에 있는 정보만으로 가치를 얻을 수 있어야 한다.

## 설계 원칙

1. **탭 확장 구조 지양**
   더보기/상세 확장 같은 계층 구조는 사용자에게 추가 행동을 요구해 거부감을 유발할 수 있으므로 배제한다. AI 판단 관련 정보는 기본 화면에서 바로 읽히도록 설계한다.

2. **정확한 수치값 미노출**
   AI 판단 문장 안에는 비중(%), 등락률, 확률 등 정확한 수치를 넣지 않는다. 정확한 수치는 그만큼의 리스크(투자 조언으로 비칠 소지)를 동반한다. 원자료 숫자는 화면 상단 헤더 영역에만 유지하고, AI 판단 문장에서는 등급·정성적 표현으로만 전달한다.
   **중요:** 이 원칙은 프롬프트 지시문("숫자 언급하지 마")에만 의존하지 않는다. Haiku에 넘기는 데이터 자체를 서버 단에서 먼저 정성적 카테고리로 변환한 뒤 전달해서, 애초에 원본 수치가 프롬프트에 존재하지 않도록 한다.

## ⚠️ 데이터 소스 정정 (2026-07-20)

이 박스는 **뉴스 텍스트를 요약/해석한 결과가 아니라, `data-ml` 저장소 LSTM 브랜치의 LSTM+XGBoost 모델 신호(이벤트 확률·방향성·경보 단계)를 반영하는 영역**이다. 초기 설계에서 이 부분을 혼동해 "뉴스 판단" 매트릭스로 잘못 설계했었고, 이번에 정정했다.

화면에는 실제로 두 개의 서로 다른 텍스트 소스가 있다.

| 화면 요소 | 데이터 소스 |
|---|---|
| "AI는 이렇게 판단했어요" 박스 | **LSTM+XGBoost 모델 신호** (이 문서의 대상) |
| 그 아래 뉴스 요약 문단("애플이 인도 내 생산 비중을...") | 24시간 yfinance 수집 + LLM 단일 요약 (별도 파이프라인, 이 문서와 무관) |

## 모델 신호 (`data-ml` LSTM 브랜치)

`shared/inference/lstm_xgb_bundle.py`의 `predict_from_feature_frames()`가 티커/날짜(as_of_date) 단위로 아래 값을 반환한다. 뉴스 기사 단위가 아니라 **하루에 한 번 도는 종목 단위 신호**다.

```
{
  "event_probability_news": 0.0~1.0,     # 뉴스 이벤트 LSTM 확률
  "event_probability_market": 0.0~1.0,   # 시장 이벤트 LSTM 확률
  "direction_probability_up": 0.0~1.0,   # XGBoost 방향성 확률
  "direction": "UP" | "DOWN",
  "active_tier": "Strong" | "High_Confidence" | "Normal" | null,
  "drawdown": float,
  "event_regime": "deep_drawdown" | "middle_drawdown" | "shallow_drawdown"
}
```

`event_regime`/`drawdown`은 `active_tier`를 계산할 때 이미 내부적으로 반영되므로(경보 임계값이 구간별로 다름), 사용자 문장에 별도로 노출할 필요는 없다.

## 원본 값 → 카테고리 변환

Haiku 프롬프트에는 아래 세 가지 카테고리만 전달한다. 원본 float은 전달하지 않는다.

| 신호 | 카테고리 | 도출 방식 |
|---|---|---|
| `alert_level` | Strong / High_Confidence / Normal | `active_tier` 값 그대로 사용 |
| `direction` | UP / DOWN | `direction` 값 그대로 사용 |
| `signal_source` | 뉴스 중심 / 시장 중심 / 복합 | `event_probability_news`와 `event_probability_market`의 차이가 마진(TBD, 예: 0.15) 이상이면 큰 쪽, 아니면 "복합" |

`active_tier`가 **null인 날은 박스 자체를 숨긴다.** 모델이 유의미한 신호를 못 찾은 날까지 문장을 억지로 만들면 "챙겨주는" 컨셉과 반대로 노이즈가 된다.

## 문장 템플릿

`[signal_source 표현] [direction 표현] 신호가 [alert_level 강도 표현] 감지됐어요.`

- signal_source 표현: 뉴스 중심 → "최근 뉴스 흐름을 중심으로" / 시장 중심 → "최근 시장 움직임을 중심으로" / 복합 → "최근 뉴스와 시장 흐름 모두에서"
- direction 표현: UP → "상승 쪽에 힘이 실리는" / DOWN → "하락 쪽에 힘이 실리는"
- alert_level 강도 표현: Strong → "강하게" / High_Confidence → "뚜렷하게" / Normal → "약하게"

예시: "최근 뉴스 흐름을 중심으로 상승 쪽에 힘이 실리는 신호가 강하게 감지됐어요."

여기에 기존 개인화 오버레이(비중 상위 3위 신호)를 뒤에 이어 붙인다: "비중이 큰 종목이라 계속 지켜볼게요." 아래 "개인화 오버레이" 절 참고.

Haiku 프롬프트 입력 예시:

```json
{
  "ticker": "AAPL",
  "alert_level": "Strong",
  "direction": "UP",
  "signal_source": "news_dominant",
  "portfolio_rank_top3": true
}
```

지시문 요지: "위 카테고리만 참고해 1~2문장의 한국어 문장을 써라. 숫자·확률·퍼센트는 절대 언급하지 마라. '~감지됐어요' 톤을 유지하고 매수/매도 등 투자 지시는 하지 마라. `portfolio_rank_top3`가 true면 마지막에 챙김 문구를 자연스럽게 덧붙여라."

**결정 (2026-07-25):** 카테고리 조합이 3(alert_level) × 2(direction) × 3(signal_source) = 18가지라 고정 템플릿만으로도 구현 가능했지만, Haiku 호출 방식으로 가기로 확정했다. 표현 다양화와 향후 카테고리 확장(신호 종류가 늘어날 가능성)을 고려한 선택. 대신 "정확한 수치값 미노출" 원칙을 지키려면 카테고리 변환을 반드시 서버 단에서 먼저 하고, Haiku에는 원본 float이 절대 들어가지 않게 해야 한다(위 원칙 2 참고).

## 개인화 오버레이 (기존 결정, 변경 없음)

### 비중 신호 (이진 판정)

절대 %가 아니라 **"보유 종목 중 비중 상위 3위 이내인가"**로 판단한다.

- 절대 % 컷오프는 분산 투자자에게 영영 뜨지 않을 수 있어 "챙겨주는" 목적에 맞지 않음
- 상대 순위가 컨셉에 더 부합
- 보유 종목이 3개 이하인 사용자는 전부 해당 (모두 챙길 만하므로 자연스러움)

### 결합 규칙

| 비중 순위 | 처리 |
|---|---|
| 상위 3위 이내 | 모델 신호 문장 뒤에 "비중이 큰 종목이라 계속 지켜볼게요." 류의 챙김 문구 추가 |
| 순위 밖 | 모델 신호 문장만 노출, 개인화 문구 없음 |

## 미확정 / 다음 단계

- `signal_source` 판정 마진값: 잠정 0.15로 두고 구현. `data/bundles/.../evaluation/`이 `.gitignore` 대상이라 실제 확률 분포를 지금 확인할 수 없었음 — 모델 팀이 백테스트 분포 갖고 있을 때 재검토 필요
- ~~Haiku 호출 vs 고정 템플릿~~ → **Haiku 호출로 확정** (2026-07-25)
- ~~뉴스 요약 문단과 모델 신호 상충 시 처리~~ → **별도 처리 불필요로 결론** (2026-07-25): signal_source를 문장에 노출하는 것 자체가 "이 박스는 뉴스 해석이 아니라 별도 패턴 신호"라는 걸 자연스럽게 구분해주므로, 두 텍스트를 각각 그대로 보여주는 것으로 충분

## 변경 이력

| 날짜 | 내용 |
|---|---|
| 2026-07-20 | 초안 작성 — 개인화 컨셉, 비중/확신도 신호, 문장 매트릭스 확정 |
| 2026-07-20 | 실제 뉴스 수집·요약 파이프라인(24시간 집계 + LLM 단일 요약) 확인. 확신도 신호는 이 구조상 도출 불가하여 제외, 개인화는 비중 순위 신호 하나로 단순화 |
| 2026-07-20 | **데이터 소스 정정**: 박스는 뉴스 텍스트 요약이 아니라 `data-ml` LSTM 브랜치의 LSTM+XGBoost 모델 신호(이벤트 확률/방향성/경보 단계) 기반. 문장 템플릿을 모델 신호 카테고리(alert_level/direction/signal_source) 기준으로 재설계. 신호 없는 날은 박스 숨김, 신호 출처는 문장에 노출하기로 결정 |
| 2026-07-25 | 연결 폴더에서 파일이 사라져 있어 재작성 (원인 미확인) |
| 2026-07-25 | 남은 미확정 사항 정리: Haiku 호출 방식 확정, signal_source 마진 0.15 잠정값(재검토 필요), 뉴스 요약과의 상충 문제는 signal_source 노출로 별도 처리 불필요 결론 |
